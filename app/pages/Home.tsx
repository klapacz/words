import * as React from 'react';

import Menu from '@/components/Menu';
import { Container } from '@/styled';

const Home: React.FC = () => (
	<Container>
		<Menu />
	</Container>
);

export default Home;
